import { FindAxisDirective } from './find-axis.directive';

describe('FindAxisDirective', () => {
  it('should create an instance', () => {
    const directive = new FindAxisDirective(null);
    expect(directive).toBeTruthy();
  });
});
