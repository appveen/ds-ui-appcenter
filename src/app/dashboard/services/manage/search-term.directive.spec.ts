import { SearchTermDirective } from './search-term.directive';

describe('SearchTermDirective', () => {
  it('should create an instance', () => {
    const directive = new SearchTermDirective();
    expect(directive).toBeTruthy();
  });
});
