import { ServiceSearchPipe } from './service-search.pipe';

describe('ServiceSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new ServiceSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
