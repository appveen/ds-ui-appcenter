import { FilterAppPipe } from 'src/app/pipes/filter-app.pipe';

describe('FilterAppPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterAppPipe();
    expect(pipe).toBeTruthy();
  });
});
