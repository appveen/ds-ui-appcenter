import { FilterIntegrationPipe } from './filter-integration.pipe';

describe('FilterIntegrationPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterIntegrationPipe();
    expect(pipe).toBeTruthy();
  });
});
